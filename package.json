{
  "name": "git-metrics",
  "scripts": {
    "serve": "esbuild --bundle --format=esm --sourcemap --servedir=. --outfile=bundle.js graph.ts",
    "build": "esbuild --bundle --format=esm --sourcemap --outfile=bundle.js graph.ts"
  },
  "dependencies": {
    "@types/d3": "^7.4.3",
    "d3": "^7.9.0",
    "esbuild": "^0.23.1",
    "typescript": "^5.6.2"
  }
}
